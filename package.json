{
  "name": "snap-machines-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @snap-machines/core && npm run build -w snap-machines-demo",
    "check": "npm run check -ws --if-present",
    "test": "npm test -ws --if-present"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
